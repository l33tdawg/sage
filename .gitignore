# Binaries
bin/
# Built binaries in repo root (not the cmd/ source dirs)
/sage-gui
/sage-lite
/sage-launcher
/amid
*.exe
*.exe~
*.dll
*.so
*.dylib

# SAGE Personal data (agent keys, memories, config)
.sage/
*.key
pending-import.json
import-done.txt

# MCP config (contains local paths)
.mcp.json

# MCP registry auth tokens (secrets — never commit)
.mcpregistry_*_token

# Test binary
*.test

# Output of go coverage
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# Vendor (Go modules) - but NOT the browser-side vendored libs, which must ship
# in-repo so the go:embed'd dashboard builds fully offline (Preact/htm + QR).
vendor/
!web/static/js/vendor/
!web/static/js/vendor/**

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# BadgerDB data
data/
*.vlog
*.sst
MANIFEST
KEYREGISTRY

# CometBFT generated configs
deploy/genesis/

# Docker
docker-compose.override.yml

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
desktop/sage-shell/target/
desktop/sage-shell/gen/
desktop/sage-shell/binaries/
.eggs/
*.egg
.venv/
venv/

# Stray test directories (real tests are in test/)
tests/

# Protobuf generated
internal/proto/sagepb/*.go

# Test artifacts
test/integration/testdata/
test/benchmark/results/

# Grafana data
deploy/monitoring/grafana/data/

# Backup files
deploy/scripts/backups/

# Claude
CLAUDE.md
.claude/

# Codex
AGENTS.md
.codex/

# Docs — docs/ is the central documentation resource. Ignore internal drafts
# (whitepapers, comms, arxiv, medium, figures) but allow the public docs.
# NOTE: the landing site (index.html etc.) now lives on the gh-pages branch.
docs/*
!docs/README.md
!docs/ARCHITECTURE.md
!docs/GETTING_STARTED.md
!docs/ADMIN_BOOTSTRAP.md
!docs/HOOKS.md
!docs/ISSUE_52_RECOVERY.md
# v11 user guides (Dhillon-approved for publication 2026-07-04; docs/README.md links them)
!docs/BRAIN.md
!docs/FEDERATION.md
!docs/ROADMAP.md
!docs/v11.9-multiprocess-fault-harness.md
!docs/desktop-shell-decision.md
!docs/native-app-daemon-contract.md
!docs/native-shell-quality-gates.md
# Agent-facing code reference (public)
!docs/reference/
# Architecture diagram (rendered PNG embedded in ARCHITECTURE.md; .mmd source kept local/untracked)
!docs/sage_architecture-17062026.png

# Node.js (Playwright e2e tests)
node_modules/
package-lock.json
test-results/
playwright-report/

# Extension build output
extension/dist/

# Papers PDFs are tracked in papers/ (not gitignored)
PHASE1_IMPLEMENTATION_PLAN.md

# ─────────────────────────────────────────────
# Level Up Integration — IP Protection
# ─────────────────────────────────────────────
# All experiment results (generated CTF challenges are proprietary outputs)
integrations/results/
integrations/levelup/results/
integrations/levelup/scripts/
# Level Up proprietary agents (already covered by app/ but be explicit)
integrations/levelup/app/
# Experiment keys and env
integrations/levelup/*.key
integrations/levelup/.env
# Backup files
integrations/levelup/results/*.sql

# SAGE bridge (Level Up IP — generic example lives in sdk/python/examples/sage_bridge_example.py)
integrations/levelup/sage_bridge.py
# Level Up-specific files (expose internal architecture)
integrations/levelup/run_experiment.py
integrations/levelup/evaluate_challenges.py
integrations/levelup/README.md
integrations/levelup/requirements.txt

# Paper 4 agents (Level Up IP — proprietary multi-agent pipeline)
integrations/levelup/paper4/

# Tracked (open-source, generic stats engine):
#   integrations/levelup/experiment_protocol.py
#   integrations/levelup/.gitignore

# Go build cache (created when GOCACHE points into the repo)
.gocache/
